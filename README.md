One-command WireGuard VPN deployment for Ubuntu servers.

Automatically installs WireGuard, configures networking, firewall rules,
creates the first client and provides a tool for adding new clients.
